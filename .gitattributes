# Normalise line endings so the repo is identical on Windows, macOS and the
# Linux builders Cloudflare Pages runs on.
* text=auto eol=lf

# Binary assets — never touch these
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.ico binary
*.woff binary
*.woff2 binary
*.pdf binary
